# MediaVore Project Roadmap

- [x] Add the time of the media in the seen DB
- [x] In the import seen, add a way to fetch the time of the media if not present
- [x] Add a button to fetch time in settings (next to the seen settings)
- [x] Media Dashboard: A "Stats" page with charts showing:
    - [x] Total time spent watching (days/hours).
    - [x] Top genres consumed.
    - [x] Top media type
    - [x] Media count by month/year.
- [x] Improve stats
  - [x] regroup by year, month, all time...
  - [x] add more stats
  - [x] add more "pages" perhaps swipe or others but not on the same screen
  - [x] Say what the values are (for genre is it number, minutes, hours  ??)
  - [x] Media Split improvements
    - [x] Add percentage to Media Split
    - [x] Give a way to select by runtime instead of by entries
  - [x] For the time frame, give the ability to select the year / the month according to the setting
  - [x] Viewing Activity logs, is a bit crowded after a while, perhaps we should be able to select the precision and the default should be readable
    - For example, 
      - if all 
        - if few years : per year
        - if few months : per month
      - if Year
        - months default
        - days if current year and year started recently
      - ...
  - [x] Fix the topbar
  - [x] Add more stats : Most seen Movie/series (in general, and most seen ep), in runtime and in logs
  - [x] Find a better way for the selection of Year / Month
  - [x] Give back the All/Year/Month selection
- [ ] Achievements: Small badges for milestones (e.g., "Cinema Buff" for 100 movies, "Binge Watcher" for finishing 5 series).
